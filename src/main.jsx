import React from 'react'
import ReactDOM from 'react-dom/client'
import MovieMatcher from './App'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <MovieMatcher />
  </React.StrictMode>,
)
